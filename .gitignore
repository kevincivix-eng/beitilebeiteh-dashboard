config.js
.DS_Store
node_modules/
.claude/serve.js

# Heavy source assets (kept locally, not needed for the site)
*.pbix
*.xlsx
Final Logo Design/
EVENT-*.csv
.claude/settings.local.json

# Raw WhatsApp exports: ~3.8GB of media plus private phone numbers and personal
# messages. Never commit — the parsed, redacted aggregates live in data/experimental/.
/whatsapp/
